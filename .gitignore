
TO FIX/FINISH from Part 1 - REPLACE FAVICON 
  - DEFAULT VALUE FOR "departs" NOT WORKING https://ga-students.slack.com/archives/C068S0D7AKC/p1705360897384229
BONUSES PART 1 - (bottom of page) 1. default departure date in "new flight" form / 2. list of flights in ascending order by dep. date, & red text if flight date has passed / 3. style index & new views

PART 2 STUFF
3  Implement the following User Story:
AAU, when viewing the list of flights, I want to click on a “detail” link displayed next to each flight to view all of the properties for that flight (show view). This show page should include each of its tickets.
4. Implement the following User Story:
AAU, when viewing the details page (show view) for a flight, I want to be able to add a ticket for that flight. Each ticket, as defined by the schema above, includes a seat and a price.
5. Implement the following User Story:
AAU, when viewing the details page (show view) for a flight, I want to see a list of that flight’s tickets (seat & price)
BONUSES
1. Style the app.
2. Add a feature to delete a flight’s *ticket*.



NEXT PUSH



QUESTIONS
-multiple css docs, i renamed "style" to "main"? missing CSS doc?
-during Define a Basic Schema for a Movie Model - required:true or no? didnt specify
  -not sure how to test when setting these up?
-slack engineering - why is my .gitignore file being uploaded? (had to set it to IGNORE)

